<tr>
    <td>
        <%= user.username %>
    </td>
    <td>
        <%= user.role %>
    </td>
    <td>
        <form action="updateUser" method="POST" class="d-inline">
            <input type="hidden" name="userId" value="<%= user._id %>">
            <button type="submit" name="action" value="Demote" class="btn btn-danger fw-medium"> Demote </button>
        </form>
        <form action="updateUser" method="POST" class="d-inline">
            <input type="hidden" name="userId" value="<%= user._id %>">
            <button type="submit" name="action" value="Promote" class="btn btn-success fw-medium"> Promote </button>
        </form>
    </td>
</tr>